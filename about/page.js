import About from "@/components/pages/About";
export const metadata = {
  title: "About",
  description:
    "Why Alza exists: most organizations don't have an HR problem — they have an alignment problem across leadership, people, operations, and culture.",
};
export default function Page() {
  return <About />;
}
